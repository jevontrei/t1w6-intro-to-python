# get the length from user
length = float(input("Enter the side length of the square: "))
print(length)

# calculate and store area in a variable3
area = length ** 2

# print the calculated area to the terminal
print(f"The area is {area})